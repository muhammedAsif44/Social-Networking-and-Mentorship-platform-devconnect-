name: Backend CI - Build & Push to ECR

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::215572308714:role/GitHubActions-ECR
          aws-region: ap-southeast-2

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push backend image
        run: |
          docker build -t devconnect-backend ./back-end
          docker tag devconnect-backend:latest 215572308714.dkr.ecr.ap-southeast-2.amazonaws.com/devconnect-backend
          docker push YOUR_ACCOUNT_ID.dkr.ecr.ap-southeast-2.amazonaws.com/devconnect-backend:latest
